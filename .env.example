# Required — powers transcription, OCR, embeddings, and ask
GEMINI_API_KEY=your-gemini-key

# Optional — for --provider elevenlabs (word-level timestamps, audio events)
# ELEVENLABS_API_KEY=your-elevenlabs-key

# Optional — for --provider deepgram (fastest transcription)
# DEEPGRAM_API_KEY=your-deepgram-key
